# Audio I/O
sounddevice==0.5.5
soundfile==0.13.1

# Wake word detection
openwakeword==0.6.0

# Voice activity detection (VAD) — conversation session mode
webrtcvad-wheels  # webrtcvad without the pkg_resources dependency (use this, not webrtcvad)

# Utilities
numpy
python-dotenv
PyYAML
huggingface_hub

# --- Optional / alternative backends ---
# faster-whisper (CPU-only on aarch64 — no CUDA via PyPI):
#   pip install faster-whisper==1.2.1
# llama-cpp-python with CUDA on Jetson — must build from source:
#   bash scripts/install_llama_cpp_python.sh
